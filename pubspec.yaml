name: panat_kas
description: Aplikasi Keuangan Panitia Natal PANAT by Natanael
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  path_provider: ^2.1.4
  share_plus: ^9.0.0
  crypto: ^3.0.3
  supabase_flutter: ^2.8.0
  pdf: ^3.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
